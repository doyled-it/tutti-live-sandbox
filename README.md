# tutti-live-sandbox

A throwaway repository for exercising the Tutti engine end to end against a real
GitHub repo and a real `claude`. Autonomous work merges into the `staging` branch,
never `main`. Safe to reset or delete at any time.
